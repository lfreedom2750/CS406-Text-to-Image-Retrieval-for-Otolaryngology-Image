training:
  batch_size: 8          # MedCLIP rất nặng
  lr: 1e-4
  epochs: 20
  temperature: 0.07
  num_workers: 4

data:
  image_root: data/processed/images
  annotation_file: data/processed/annotations.json
